# Bake outputs — too large for git, live on GitHub Releases.
bake-sample/
bake-output/
.lidar-cache/

# manifest.json built locally for testing
/manifest.json

# Geonames raw dump — regenerable via `select_tiles.py --refresh`.
data/_geonames_cache.json

# Python cruft.
__pycache__/
*.pyc
.venv/
.python-version
.DS_Store
